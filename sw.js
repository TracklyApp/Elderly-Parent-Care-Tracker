const CACHE='kindred-shell-v12';
const SHELL=['/','/index.html','/storage.js','/database.js','/bootstrap.js','/guide.js','/guide-data.js','/guide.css','/output/pdf/Kindred-User-Guide.pdf','/style.css','/mobile.css','/design.css','/design.js','/premium.js','/premium-core.js','/premium.css','/mobile.js','/theme.css','/theme.js','/examples-data.js','/examples.js','/app.js','/care-core.js','/enhancements-core.js','/family.js','/enhancements.js','/manifest.webmanifest','/icon.svg','/icon-192.png','/icon-512.png'];
self.addEventListener('install',event=>{event.waitUntil(caches.open(CACHE).then(cache=>cache.addAll(SHELL)).then(()=>self.skipWaiting()));});
self.addEventListener('activate',event=>{event.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(k=>k.startsWith('kindred-shell-')&&k!==CACHE).map(k=>caches.delete(k)))).then(()=>self.clients.claim()));});
self.addEventListener('fetch',event=>{const url=new URL(event.request.url);if(event.request.method!=='GET'||url.origin!==self.location.origin||url.pathname.startsWith('/api/'))return;if(!SHELL.includes(url.pathname))return;event.respondWith(fetch(event.request).then(response=>{if(response.ok){const copy=response.clone();caches.open(CACHE).then(cache=>cache.put(event.request,copy));}return response;}).catch(()=>caches.match(event.request).then(cached=>cached||(event.request.mode==='navigate'?caches.match('/'):Response.error()))));});
self.addEventListener('push',event=>{let data={};try{data=event.data?.json()||{};}catch{}event.waitUntil(self.registration.showNotification('Kindred care reminder',{body:data.body||'A care activity needs your attention. Open Kindred to review it.',icon:'/icon-192.png',badge:'/icon-192.png',tag:data.tag||'kindred-reminder',data:{url:'/'}}));});
self.addEventListener('notificationclick',event=>{event.notification.close();event.waitUntil(self.clients.matchAll({type:'window',includeUncontrolled:true}).then(windows=>{const app=windows.find(w=>new URL(w.url).origin===self.location.origin);return app?app.focus():self.clients.openWindow('/');}));});
